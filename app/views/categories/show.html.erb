<div class="is-flex flex-direction-column flex-grow-1">
  <header class="header">
		<%= link_to categories_path, class: "button" do %>
      <span class="material-icons-outlined">arrow_back</span>
    <% end %>
    <div class="is-flex flex-grow-1 justify-content-center">
      <h1 class="title">Transactions</h1>
    </div>
  </header>
	<p>$<%= @category.total_amount %></p>
	<%= render 'deals/index', locals: { deals: @deals } %>
</div>
<footer class="p-4 is-secondary">
  <%= link_to 'Add a new transaction', new_deal_path, class: "button is-outlined" %>
</footer>