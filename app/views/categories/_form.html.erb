<div class="has-background-color is-flex flex-grow-1 flex-direction-column">
  <%= form_with(model: category) do |form| %>
    <div class="header">
      <%= render "devise/shared/back_button" %>
      <div class="is-flex flex-grow-1 justify-content-center">
        <h2 class="title">New Category</h2>
      </div>
      <div class="actions">
        <%= form.submit "Create", class: "button is-link has-text-white" %>
      </div>
    </div>
    <%= render "devise/shared/error_messages", resource: category %>
    <div class="input-group my-2 shadow-box">
      <div class="field">
        <%= form.text_field :name, autofocus: true, placeholder: "Name", class: "input" %>
      </div>
      <div class="divider"></div>
      <div class="field">
        <%= form.text_field :icon, placeholder: "Icon URL", class: "input" %>
      </div>
    </div>
  <% end %>
</div>
